{
  "name": "orderflowx",
  "version": "0.1.0",
  "private": true,
  "description": "Distributed order reservation system focused on idempotency, consistency, and failure recovery.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "jest --runInBand",
    "benchmark": "node scripts/run-benchmark.js"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^22.10.1",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "zod": "^3.23.8"
  }
}
