{
  "name": "orderflowx",
  "version": "0.1.0",
  "private": true,
  "description": "Distributed order reservation system focused on idempotency, consistency, and failure recovery.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "jest --runInBand",
    "benchmark": "npm run build && node dist/src/bench/benchmark.js",
    "start": "node dist/src/index.js",
    "dev": "node dist/src/index.js"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^22.10.1",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "main": "dist/src/index.js"
}
